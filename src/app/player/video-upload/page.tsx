import { PlayerLayout } from "@/layout";
import { VideoUploadPage } from "@/views";
import React from "react";

const Profile = () => {
  return (
    <PlayerLayout>
      <VideoUploadPage />
    </PlayerLayout>
  );
};

export default Profile;

import { PlayerLayout } from "@/layout";
import { AccountSetting } from "@/views";
import React from "react";

const Settings = () => {
  return (
    <PlayerLayout>
      <AccountSetting />
    </PlayerLayout>
  );
};

export default Settings;
